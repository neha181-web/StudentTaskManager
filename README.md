# Student Task Manager (MERN Stack)

A complete Full-Stack Task Management application.

## 🚀 Features (Documentation Compliant)
- **Create**: Add tasks with title and description.
- **Read**: Fetch all tasks from MongoDB.
- **Update (Full)**: Edit existing task details via PUT route.
- **Update (Partial)**: Toggle completion status via PATCH route.
- **Delete**: Remove tasks from the database.

## 🛠️ Tech Stack
- **Frontend**: React.js, Axios
- **Backend**: Node.js, Express.js
- **Database**: MongoDB Atlas

## 📝 Setup Instructions
1. Clone the repo.
2. Inside `backend/backend`, run `node index.js` to start the server.
3. Inside the frontend folder, run `npm run dev`.
